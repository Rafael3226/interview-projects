const host = "https://jsonplaceholder.typicode.com";

const URIs = {
  POSTS: `${host}/posts`,
  USERS: `${host}/users`,
  COMMENTS: `${host}/comments`,
  GUIDE: `${host}/guide`,
};

export default URIs;
